<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ToolbarBase" parent="@style/Widget.AppCompat.Toolbar">
        <item name="android:textColor">@color/white</item>
        <item name="android:minHeight">@dimen/toolbar_height</item>
        <item name="contentInsetStart">0.0dip</item>
    </style>

    <style name="medal_dialog_style" parent="@style/AppTheme">
        <item name="android:windowBackground">@color/half_trans</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="toolbarStyle">@style/ToolbarBase</item>
        <item name="colorPrimaryDark">@color/half_trans</item>
    </style>
    <style name="medal_wall_progress_style" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:maxHeight">5.0dip</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:progressDrawable">@drawable/medal_wall_progress</item>
        <item name="android:minHeight">5.0dip</item>
    </style>

    <style name="AppTheme.Translucent.MedalDetail" parent="@style/AppTheme">
        <item name="android:windowBackground">@color/trans</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="MedalTitleTextStyle">
        <item name="android:textSize">15.0sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
